<UserControl x:Class="Wind.Views.Settings.ProcessInfoPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <UserControl.Resources>
        <!-- GridViewColumnHeader: Fluent theme style -->
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridViewColumnHeader">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlFillColorTertiaryBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListViewItem: Fluent theme style -->
        <Style TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="0,0,0,1"
                                Padding="0,2">
                            <GridViewRowPresenter VerticalAlignment="Center"
                                                  Margin="0,2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background"
                                        Value="{DynamicResource SubtleFillColorSecondaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background"
                                        Value="{DynamicResource SubtleFillColorTertiaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="16,12,16,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Embedded Processes" FontSize="14" FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
            <ui:Button Grid.Column="1" Content="Refresh"
                      Icon="{ui:SymbolIcon ArrowSync24}"
                      Command="{Binding RefreshCommand}"
                      Appearance="Secondary" FontSize="11" Padding="8,2"/>
        </Grid>

        <!-- Search -->
        <ui:TextBox Grid.Row="1" Margin="0,0,0,8"
                    PlaceholderText="Filter processes..."
                    Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                    Icon="{ui:SymbolIcon Search24}"/>

        <!-- Process ListView -->
        <Border Grid.Row="2" Background="{DynamicResource ControlFillColorDefaultBrush}"
                CornerRadius="6" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1" ClipToBounds="True">
            <ListView x:Name="ProcessListView"
                      ItemsSource="{Binding ProcessesView}"
                      Background="Transparent"
                      BorderThickness="0"
                      SelectionMode="Single"
                      GridViewColumnHeader.Click="GridViewColumnHeader_Click"
                      Visibility="{Binding HasProcesses, Converter={StaticResource BoolToVisibilityConverter}}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Title" Width="180"
                                        DisplayMemberBinding="{Binding DisplayTitle}"/>
                        <GridViewColumn Header="Process" Width="120"
                                        DisplayMemberBinding="{Binding ProcessName}"/>
                        <GridViewColumn Header="PID" Width="60"
                                        DisplayMemberBinding="{Binding ProcessId}"/>
                        <GridViewColumn Header="Memory" Width="80"
                                        DisplayMemberBinding="{Binding MemoryUsage}"/>
                        <GridViewColumn Header="Started" Width="140"
                                        DisplayMemberBinding="{Binding StartTime}"/>
                        <GridViewColumn Header="Path" Width="250"
                                        DisplayMemberBinding="{Binding ExecutablePath}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <!-- Empty state -->
        <TextBlock Grid.Row="2" Text="No embedded processes"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   Visibility="{Binding HasProcesses, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=invert}"/>

        <!-- Status bar -->
        <TextBlock Grid.Row="3" Margin="0,6,0,0"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="11">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="{Binding HasProcesses, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </Style>
            </TextBlock.Style>
            <Run Text="Click column headers to sort"/>
        </TextBlock>
    </Grid>
</UserControl>

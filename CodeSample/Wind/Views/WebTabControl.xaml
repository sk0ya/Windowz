<UserControl x:Class="Wind.Views.WebTabControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Address Bar -->
        <Border Grid.Row="0"
                Background="{DynamicResource ControlFillColorDefaultBrush}"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="0,0,0,1"
                Padding="4,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" x:Name="BackButton" Click="BackButton_Click"
                        Width="30" Height="30" Padding="0"
                        Background="Transparent" BorderThickness="0"
                        Cursor="Hand" ToolTip="戻る">
                    <ui:SymbolIcon Symbol="ArrowLeft24" FontSize="14"/>
                </Button>
                <Button Grid.Column="1" x:Name="ForwardButton" Click="ForwardButton_Click"
                        Width="30" Height="30" Padding="0"
                        Background="Transparent" BorderThickness="0"
                        Cursor="Hand" ToolTip="進む">
                    <ui:SymbolIcon Symbol="ArrowRight24" FontSize="14"/>
                </Button>
                <Button Grid.Column="2" x:Name="RefreshButton" Click="RefreshButton_Click"
                        Width="30" Height="30" Padding="0"
                        Background="Transparent" BorderThickness="0"
                        Cursor="Hand" ToolTip="更新">
                    <ui:SymbolIcon Symbol="ArrowClockwise24" FontSize="14"/>
                </Button>
                <ui:TextBox Grid.Column="3" x:Name="UrlTextBox"
                            KeyDown="UrlTextBox_KeyDown"
                            VerticalContentAlignment="Center"
                            FontSize="13"
                            Margin="4,0,0,0"
                            PlaceholderText="URLを入力"/>
            </Grid>
        </Border>

        <!-- WebView2 -->
        <wv2:WebView2 x:Name="WebView" Grid.Row="1"/>
    </Grid>
</UserControl>
